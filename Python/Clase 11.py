import math

#En Python se comenta con el numeral y se acaba el comentario al pasar a la siguiente linea
#En la clase 11 de el curso básico de Python enseña sobre la reconversión de variables con los siguientes comandos
    #Input("") para pedirle al usuario que introduzca datos.
    #int() con datos o variables dentro de parentesis para convertirlo en número entero.
    #str() para convertir números tanto decimales como enteros a strings.
    
#El siguiente es un ejemplo de buenas practicas al escribir variables:

num = int(input('Escribe un número: '))
print(num)